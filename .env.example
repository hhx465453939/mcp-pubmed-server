# PubMed Data Server 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ==================== 必需配置 ====================
# PubMed API 配置
PUBMED_API_KEY=你的NCBI_API密钥
PUBMED_EMAIL=你的邮箱地址

# ==================== 功能配置 ====================
# 摘要截断模式：quick | deep
# quick：1500 字符（快速检索，可能不包含完整摘要）
# deep：6000 字符（深度检索；建议模型上下文窗口 ≥ 120k tokens）
ABSTRACT_MODE=quick

# 全文模式：disabled | enabled | auto
# disabled：禁用全文功能（默认）
# enabled：启用全文检测，手动下载
# auto：启用全文检测，自动下载可用的OA论文
FULLTEXT_MODE=disabled

# EndNote导出：enabled | disabled
# enabled：自动导出RIS和BibTeX格式（默认）
# disabled：禁用EndNote导出
ENDNOTE_EXPORT=enabled

# ==================== 代理配置 ====================
# 代理开关：enabled | disabled
# enabled：启用代理功能
# disabled：禁用代理功能（默认）
PROXY_ENABLED=disabled

# HTTP代理：用于HTTP和HTTPS请求
# 格式：http://proxy.example.com:8080
# 支持认证：http://username:password@proxy.example.com:8080
HTTP_PROXY=

# HTTPS代理：专门用于HTTPS请求
# 格式：https://proxy.example.com:8080
# 支持认证：https://username:password@proxy.example.com:8080
HTTPS_PROXY=

# 代理认证（可选）
# 如果代理需要认证，请填写用户名和密码
PROXY_USERNAME=
PROXY_PASSWORD=

# 代理超时设置（秒）
PROXY_TIMEOUT=30

# 代理重试次数
PROXY_RETRY_COUNT=3
